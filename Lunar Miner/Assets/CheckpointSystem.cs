using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CheckpointSystem : MonoBehaviour
{
    private void OnCollisionEnter(Collision other)
    {
        /*f (other.gameObject.CompareTag("Player"))
        {
            other.transform.position=new Vector3(transform.position.x+ 0.824f, transform.position.y+2f,other.transform.position.z);
        }*/
    }
}
